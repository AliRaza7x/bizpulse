# Copy to .env and adjust if needed. .env is gitignored — never commit it.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=bizpulse_root_pw
DB_NAME=bizpulse

# Added: broker address for kafka/producer.py and kafka/consumer.py
KAFKA_BROKER=localhost:9094
